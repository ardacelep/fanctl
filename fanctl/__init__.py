"""fanctl — control Levoit (VeSync) tower fans from the desktop, web, or CLI.

The package is split into a UI-agnostic ``backend`` (device + auth logic) and
swappable ``ui`` frontends (Flet, tkinter). See ARCHITECTURE.md.
"""

__version__ = "0.1.0"
